# OS / editor
.DS_Store
.env

# Python build / caches
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Virtual environments
.venv/

# Test / coverage / linters
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/

# Project-local databases
*.db

# Scratch / drafts / planning — gitignored per operantkit policy
# Package-local scratch / planning / draft / review traces
.local/
tmp/
PLAN.md

# uv package manager is banned; any accidental uv.lock must not be tracked.
uv.lock

# hatch-vcs generated version file (derived from git tag)
src/contingency_procedure2dsl/_version.py
.venv-fresh/
